<%= form_with model: [@produto, Comment.new], html: {class: "produto_search" } do |f| %>
  <div class="row">
    <div class="col-lg-12">
      <%= f.label 'Comentário', class: 'text-white' %>
      <%= f.text_area :commenter, class: 'form-control' %><br/>
    </div>
    
    <div class="form-group col-md-5">
      <%= f.label :classificacao, class: 'text-white' %>
      <%= f.select :classificacao, Comment.classificacaos.keys.to_a, {}, class: "form-control" %>
    </div>

    <div class="form-group col-md-5">
      <%= f.label :status, class: 'text-white'%><br/>
      <%= f.select :status, Comment.statuses.keys.to_a, {}, class: "form-control" %>
    </div>
    <br/>
  </div>
  <br/>
  <br/>  
  <%= f.submit "Criar Comentario", class: "btn btn-primary text-white" %>
<% end %>